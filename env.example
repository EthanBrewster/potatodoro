# Server Configuration
PORT=3000
NODE_ENV=development

# Redis Connection
# For local development with docker-compose:
REDIS_URL=redis://localhost:6379
# For Railway: Use the Redis plugin URL (automatically set)

# PostgreSQL Connection
# For local development with docker-compose:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hotpotato
# For Railway: Use the Postgres plugin URL (automatically set)

# CORS Configuration
# For local development:
CORS_ORIGIN=http://localhost:5173
# For production: Set to your Railway app URL
# CORS_ORIGIN=https://your-app.railway.app
